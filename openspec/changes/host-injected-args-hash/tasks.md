# Tasks

- [x] OpenSpec change (proposal, design, delta specs)
- [x] Host-bind helper: extract `_meta` / `host_bound`, override deny, merge hash
- [x] Wire into `inspectCall` (evaluate / issue / authorize)
- [x] Policy `host_bound_fields` parse + MCP Zod schemas
- [x] Example policy for host-bound tenant/index
- [x] Tests: merge hash, model override deny, allowlist still not a grant
- [x] README cookbook + gateway `_meta` contract (no hard dependency)
- [x] `npm test`
